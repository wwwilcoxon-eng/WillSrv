curl -fsSL https://packages.playit.gg/install.sh | bash 

sudo playitd --socket-path /run/playit/playitd.sock 
sudo playit --socket-path /run/playit/playitd.sock 
java -Xms8G -Xmx12G -Dcom.mojang.eula.agree=true -jar server.jar --nogui --bonusChest

drake-sizable.tun.ply.gg

rm -rf .git && git init && git branch -M main && git add . && git commit -m "Día 5" && git remote add origin https://github.com/wwwilcoxon-eng/WillSrv && git push -f origin main